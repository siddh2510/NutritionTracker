services:
  - type: static_site
    name: nutrition-tracker
    env: static
    buildCommand: 'npm run build'  # or 'yarn build' if using yarn
    publishDirectory: 'build'
    staticPublishPath: '/'
    github:
      repo: 'siddh2510/NutritionTracker'
    branch: 'main'
